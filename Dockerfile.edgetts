FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir edge-tts fastapi uvicorn[standard] pydantic

COPY scripts/edgetts_server.py /app/server.py

ENV HOST=0.0.0.0 \
    PORT=8766 \
    EDGETTS_CACHE_DIR=/data/cache

RUN mkdir -p /data/cache

EXPOSE 8766

CMD ["python", "server.py"]

beautifulsoup4
boto3
ebooklib
fastapi
fsrs>=6.3.1,<7
httpx
lxml
nltk
python-dotenv
python-multipart
pypdf
pymupdf
# Optional local fallback for unusual PDFs. Do not install on Vercel by default:
# it pulls in Pillow/pdfminer/pypdfium2/cryptography and can push the Python
# function bundle over Vercel's size limit. pdf_to_audio.py falls back to pypdf
# when this import is unavailable.
# pdfplumber
psycopg[binary]>=3.2,<4
# sentry-sdk[fastapi]>=2.0,<3  — excluded: Vercel Lambda is at the 245 MB size limit.
# To enable Sentry on Vercel, add it via Vercel's native Sentry integration instead
# (https://vercel.com/integrations/sentry) which injects the DSN without adding package size.
# For local / self-hosted deployments, uncomment the line above and set SENTRY_DSN in .env.
uvicorn[standard]

PyJWT>=2.8,<3
